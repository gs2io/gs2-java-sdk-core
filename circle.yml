## Customize the test machine
machine:
  java:
    version: oraclejdk8

test:
  post:
    - cp ./target/*-javadoc.jar $CIRCLE_ARTIFACTS

deployment:
  maven:
    branch: develop
    commands:
      - mvn deploy -DperformRelease=true